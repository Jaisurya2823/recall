[project]
name = "recall"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "JAISURYA P", email = "jaisurya2823@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "boto3>=1.34",
    "psycopg2-binary>=2.9",
    "python-dotenv>=1.0",
    "rich>=13.0",
]

[project.scripts]
recall = "recall:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
